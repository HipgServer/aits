# MongoDB Connection String for Production / Vercel (MongoDB Atlas required for Vercel)
# Replace <username>, <password>, and <cluster-url> with your MongoDB Atlas credentials
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/accident_tracking_db?retryWrites=true&w=majority
